# TASK-001 同步最终用户与维护文档

## 状态

completed

## 修改

- `README.md` 增加 DrissionPage `4.2.0b3` 最终迁移状态、Edge smoke 通过和 userscript `7 pass, 0 fail`。
- `CLAUDE.md` 将 Phase 3 验证说明从边界描述更新为已通过事实，并保留 `use_system_user.py` 写本机配置说明。
- `.workflow/project.md` 增加 DrissionPage 4.2 迁移验证状态和关键决策。

## 验证

文档收敛检查覆盖 `4.2.0b3`、Phase 3、Edge smoke、userscript 和 `use_system_user` 边界。
